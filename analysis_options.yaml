include: package:flutter_lints/flutter.yaml

analyzer:
  errors:
    # Treat unawaited futures as an error, not just a hint
    unawaited_futures: error
    # Treat missing returns as an error
    missing_return: error

  # Relax style-only rules in test/ — mocktail patterns require closures
  # and test constructors often pass explicit defaults for readability.
  exclude:
    - "**/*.g.dart"
    - "**/*.freezed.dart"

linter:
  rules:
    # --- Code style & correctness ---
    prefer_const_constructors: true
    prefer_const_declarations: true
    prefer_const_literals_to_create_immutables: true
    prefer_final_locals: true
    prefer_final_in_for_each: true

    # --- Imports ---
    always_use_package_imports: true

    # --- Logging / debug ---
    avoid_print: true          # force use of debugPrint or a logger

    # --- Async safety ---
    unawaited_futures: true
    avoid_void_async: true

    # --- Null safety & type safety ---
    cast_nullable_to_non_nullable: true
    avoid_dynamic_calls: true
    unnecessary_nullable_for_final_variable_declarations: true

    # --- Dead code & clarity ---
    unnecessary_statements: true
    unnecessary_lambdas: true

    # --- Flutter-specific ---
    use_build_context_synchronously: true
    sized_box_for_whitespace: true
    use_full_hex_values_for_flutter_colors: true
